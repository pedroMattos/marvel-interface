import { FormContainer, FormTitle } from "../../views.styles";

export default function LoginForm() {
  return (
    <FormContainer>
      <FormTitle $highLightText=".">Bem-vindo</FormTitle>
      <p>Informe as suas credenciais de acesso ao portal</p>
    </FormContainer>
  )
}